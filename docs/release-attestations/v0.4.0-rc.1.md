# DSH Orbit v0.4.0-rc.1 Release Candidate Attestation

Status: **HOLD — mounted two-node smoke blocked by unavailable Docker Linux engine; Final Review cannot PASS**
SOP: `docs/sop/v0.4-endpoint-selector-multistage-sop.md`, Stage 8
Parent Stage 7 Baseline: `b8bdbbda91180b91005f9344aa28fa029cf37df2` (E7.2)
Executable Candidate (C8.2.1): `73f6d7d6a8518200a9b522a2a5706d36af992190`
Evidence Closure: this document and `test/evidence/stage8/` are the direct E8.2 evidence-only descendant of C8.2.1; the release contract binds the closure identity to the current HEAD.
Release Candidate Tag: `v0.4.0-rc.1` (**NOT CREATED**)
Stage 8: **HOLD — physical mounted gate not executed**

## 1. Provenance & Ancestry

- Branch: `chore/v0.4-stage8-release-closure`
- Stage 7 parent: `b8bdbbda91180b91005f9344aa28fa029cf37df2`
- C8.2.1 executable candidate: `73f6d7d6a8518200a9b522a2a5706d36af992190`
  - Contract-only construction commit; no runtime code, package version, or evidence changes.
  - Candidate is a full Git commit descended from the accepted C8 lineage.
- Evidence execution window: `2026-09-07T08:45:32Z` through `2026-09-07T08:45:33Z`, after C8.2.1 and before E8.2.
- E8.2 will be a direct docs/evidence-only child of C8.2.1.
- Mechanical release contract validates:
  - executable candidate = `parent(HEAD)`;
  - release closure = `HEAD`;
  - release closure parent = executable candidate;
  - manifest/artifact candidate binding;
  - artifact SHA-256 and byte counts;
  - candidate timestamp < every evidence `executedAt` < closure timestamp;
  - mounted result and HOLD semantics fail closed.

## 2. Fixed External DSH Baseline

- Version: `0.1.1-rc.2`
- Git SHA: `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`
- CLI SHA-256: `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`

## 3. Automated Gates

| Gate | Result |
| :--- | :--- |
| Public Tree | **PASS** |
| Legacy Stage 8 release contract | **PASS** |
| v0.4 provenance release contract | **PASS** |
| Full `npm run check` | **PASS** — 390 total / 385 pass / 0 fail / 5 skip |
| `git diff --check` | **PASS** |
| Pinned Real DSH | **PASS** — 1 pass / 0 fail / 0 skip |
| Docker mounted driver | **BLOCKED** — Docker Linux engine unavailable |

## 4. Evidence Summary

- `fresh-install.json`: **PASS**, local schema 5 install, enrollment, report, route target.
- `migration.json`: **PASS**, real local v3 fixture migration to schema 5 with route tables and integrity checks.
- `backup-restore.json`: **PASS**, real local WAL `VACUUM INTO` backup/mutate/quiesced restore with route identity recovery.
- `two-node-mounted-smoke.json`: **BLOCKED / NOT_EXECUTED**. The required Docker driver could not start because `docker info` could not connect to `npipe:////./pipe/dockerDesktopLinuxEngine`. All mounted matrix fields remain `NOT_EXECUTED`.
- `promotion-plan-validation.json`: **PLAN ONLY**. Production promotion and staging rollback execution were not performed.
- `manifest.json`: SHA-256 and byte counts match all five artifacts and bind them to C8.2.1.

## 5. Scope and Limitations

- Reverse-connected nodes, NAT traversal, reverse tunnels, fleet execution, `terminal.pty`, and `agents.run` are not part of v0.4 and remain outside this release candidate.
- RouteIngress nonce replay cache is process-local. Restart resets the cache; the bounded security boundary is signed proof, the 30-second timestamp skew window, and the single-Hub assumption.
- Node failure fails closed; there is no silent failover.
- Routing requires wildcard DNS, wildcard TLS, an authenticated outer gateway, and preserved `Host` headers.

## 6. Promotion and Rollback

- Production promotion: **NOT EXECUTED**.
- Release publication: **NOT PUBLISHED**.
- Tag: **NOT CREATED**.
- Rollback remains gateway/DNS mapping only; Registry, node IDs, route targets, and identities are preserved.

## 7. Final Review Status

- Stage 8: **HOLD — mounted two-node smoke requires Docker engine recovery and fresh execution**
- Final Review: **NOT AUTHORIZED TO PASS**
