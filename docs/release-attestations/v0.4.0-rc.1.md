# DSH Orbit v0.4.0-rc.1 Release Candidate Attestation

Status: **HOLD — mounted two-node smoke did not reach the independently observed lifecycle checkpoint; Final Review cannot PASS**
SOP: Stage 8 release closure
Parent Stage 7 Baseline: `b8bdbbda91180b91005f9344aa28fa029cf37df2` (E7.2)
Executable Candidate (C8.2.2): `90312b66ce6f084ae10890313d71cd4bc19244f2`
Evidence Closure: E8.3 — this document and `test/evidence/stage8/` are the direct evidence-only closure child of the executable candidate.
Release Candidate Tag: `v0.4.0-rc.1` (**NOT CREATED**)
Stage 8: **HOLD — physical mounted gate not executed**

## 1. Provenance and chronology

- Branch: `chore/v0.4-stage8-release-closure`
- Accepted Stage 7 evidence baseline: `b8bdbbda91180b91005f9344aa28fa029cf37df2`
- Executable candidate: `90312b66ce6f084ae10890313d71cd4bc19244f2`
  - Contract-only C8.2.2 commit; it removes the impossible attestation self-reference requirement.
  - It is descended from the accepted Stage 8 construction line and contains no runtime feature work.
- Evidence execution timestamp: `2026-09-07T10:48:26.510Z`, after the candidate commit and before this evidence closure commit.
- Mechanical contract checks:
  - `releaseClosureCommit = HEAD` and `executableCandidateCommit = HEAD^`;
  - `parent(HEAD) === HEAD^`;
  - candidate SHA appears in the attestation, manifest, and every artifact;
  - every artifact SHA-256 and byte count matches `manifest.json`;
  - candidate commit time precedes every artifact `executedAt`, which precedes the closure commit time;
  - `HEAD^..HEAD` is restricted to this attestation and `test/evidence/stage8/**`;
  - mounted non-PASS remains fail-closed as HOLD.

## 2. Fixed external DSH baseline

- Version: `0.1.1-rc.2`
- Git SHA: `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`
- CLI SHA-256: `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`

## 3. Automated and local acceptance gates

| Gate | Result |
| :--- | :--- |
| Public Tree | **PASS** |
| Pinned Real DSH | **PASS** — `0.1.1-rc.2` baseline unchanged |
| Local migration/backup acceptance subset | **PASS** — 41 pass / 0 fail / 2 skip |
| Full regression | **PASS** — 392 total / 387 pass / 0 fail / 5 skip |
| `git diff --check` | **PASS** before closure |
| Docker mounted driver | **BLOCKED** — runner exited before enrollment/lifecycle barrier |

## 4. E8.3 evidence summary

- `fresh-install.json`: **PASS**, local file-backed Registry/schema and lifecycle acceptance subset rerun against candidate `90312b66ce6f084ae10890313d71cd4bc19244f2`.
- `migration.json`: **PASS**, v3 → v4 → v5 migration, integrity, route tables, identity preservation, and idempotent rerun covered by the local acceptance subset.
- `backup-restore.json`: **PASS**, WAL-backed `VACUUM INTO`, quiesced atomic restore, sidecar quarantine, route identity recovery, and private-key exclusion covered by the local acceptance subset.
- `promotion-plan-validation.json`: **PLAN ONLY**. Production promotion, release publication, DNS/gateway cutover, and rollback execution were not performed.
- `two-node-mounted-smoke.json`: **BLOCKED / NOT_EXECUTED**. Docker Linux Engine was available and the Firefox page showed the Registry UI, but the runner exited before enrollment and no current-run node IDs or lifecycle checkpoint were independently observed. Historical Registry nodes were rejected. The mounted matrix remains `NOT_EXECUTED`.
- `manifest.json`: binds all five artifact hashes and byte counts to candidate `90312b66ce6f084ae10890313d71cd4bc19244f2`; physical gate remains blocked.

## 5. Scope and limitations

- Reverse-connected nodes, NAT traversal, reverse tunnels, fleet execution, `terminal.pty`, and `agents.run` are not part of v0.4 and remain outside this release candidate.
- RouteIngress nonce replay cache is process-local. Restart resets the cache; the bounded security boundary is signed proof, the 30-second timestamp skew window, and the single-Hub assumption.
- Node failure fails closed; there is no silent failover.
- Routing requires wildcard DNS, wildcard TLS, an authenticated outer gateway, and preserved `Host` headers.

## 6. Promotion and rollback

- Production promotion: **NOT EXECUTED**.
- Release publication: **NOT PUBLISHED**.
- Tag: **NOT CREATED**.
- Rollback remains gateway/DNS mapping only; Registry, node IDs, route targets, and identities are preserved.

## 7. Final review status

- Stage 8: **HOLD — mounted two-node smoke requires a fresh runner-owned browser lifecycle checkpoint and complete execution**
- Final Review: **NOT AUTHORIZED TO PASS**
