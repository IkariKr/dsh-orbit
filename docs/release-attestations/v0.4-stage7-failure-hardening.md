# DSH Orbit v0.4 Stage 7 Failure, Restart & Compatibility Hardening Attestation

Status: **COMPLETE — awaiting independent Stage 7 review**
SOP: `docs/sop/v0.4-endpoint-selector-multistage-sop.md`, Stage 7
Stage 6 Baseline: `e8bb6a6538439ed7a89b98e7812a02dda0468759` (E6.6)
Candidate Freeze (C7.1): `2559a17ed6e7ff0cbe58f1b45d40e3b166eb0582`
Stage 7: **COMPLETE — awaiting independent Stage 7 review**
Stage 8: **NOT STARTED**

This release attestation records the official implementation, verification, and executable evidence for Stage 7 (Failure, Restart & Compatibility Hardening) of DSH Orbit v0.4 Endpoint Selector.

---

## 1. Candidate Freeze & Provenance Chain

- **Construction Branch**: `chore/v0.4-stage7-failure-hardening`
- **Stage 6 Baseline (E6.6)**: `e8bb6a6538439ed7a89b98e7812a02dda0468759`
- **Candidate Code Freeze (C7.1)**: `2559a17ed6e7ff0cbe58f1b45d40e3b166eb0582`
  - Commit Timestamp: `2026-09-07T02:07:52Z` (`10:07:52 +0800`)
  - Commit Message: `fix(stage7): check own property on compatibility profiles and retain boolean evidence (C7.1)`
  - Worktree state at rehearsal: clean
  - Fixes implemented:
    - P1-1: Used `Object.hasOwn(compatibilityProfiles, dshVersion)` to block prototype property resolution (`toString`, `constructor`, `__proto__`, `hasOwnProperty`, `valueOf`). Added negative test assertions.
    - P2-3: Scrubber in `registry-stage7-drill.mjs` updated to retain safety boolean assertions (`privateKeyExcludedFromDigest`, `privateKeyExcludedFromInspection`) while stripping secret values.
- **Physical Two-Host Rehearsal Window**:
  - Rehearsal Start: `2026-09-07T02:15:00Z`
  - Rehearsal Completion: `2026-09-07T02:45:00Z`
  - Total Duration: 1800 seconds (30 minutes)
- **Strict Chronology Invariant**:
  $$\text{C7.1 Commit (02:07:52Z)} < \text{Rehearsal Window (02:15:00Z - 02:45:00Z)} < \text{E7.2 Commit (>02:48:00Z)}$$
- **Evidence-Only Commit (E7.2)**: Bound directly to candidate `2559a17ed6e7ff0cbe58f1b45d40e3b166eb0582`.

---

## 2. External Pinned Harness Baseline

In strict accordance with the Stage 7 Construction Order, the external DeepSeek Harness baseline remains frozen:

- **DSH Version**: `0.1.1-rc.2`
- **Git Commit SHA**: `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`
- **CLI Binary SHA-256**: `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`

---

## 3. Hardening Matrix & Predicate Audit (S7-F1 through S7-F13)

| Area | Requirement | Verification Method & Sub-Artifact | Status |
| :--- | :--- | :--- | :--- |
| **S7-F1** | v0.3 -> v0.4 Registry Migration | Real migrations from v1, v2, v3, v4 databases to v5 schema; `route_targets` and `hub_route_keys` added; reachable domain `unknown\|ok\|unreachable`. | PASS |
| **S7-F2** | Route Identity Backup / Restore | `VACUUM INTO` backup -> state mutation -> writer-quiesced restore restores exact pre-mutation `route_targets` and `hub_route_keys` without private key leakage in state digests. | PASS |
| **S7-F3** | Secret-Bearing Registry File Protection | POSIX `0600` enforced on database, WAL, and VACUUM files; documented Windows ACL expectation. | PASS |
| **S7-F4** | Inspection & Evidence Safety | Private keys and private-key hashes are excluded from `safeState()`, deterministic state digests, and inspection outputs. Boolean assertions preserved. | PASS |
| **S7-F5** | Route / Key Persistence Corruption | Corrupt schema/data in `route_targets` or `hub_route_keys` fails closed on startup without silent table recreation or rebuild. | PASS |
| **S7-F6** | Hub Route Key Rotation & Overlap | Key rotation overlap is preserved across Hub process restarts; old keys remain active during transition and revoke predictably. Documented with authentic keyIds in `route-key-rotation.json`. | PASS |
| **S7-F7** | Nonce Restart Semantics | Process-local in-memory nonce cache rejects replays within process (401 `replay`); bounded replay protection across restarts via timestamp skew window. | PASS |
| **S7-F8** | TLS Trust Failure Matrix | Unknown CA and mismatched SAN fail closed; no TLS bypass (`rejectUnauthorized: false` prohibited across all code and tests). Documented with verified leaf cert matching Stage 6 Rehearsal CA in `tls-failure.json`. | PASS |
| **S7-F9** | Stale Compatibility & Capability Withdrawal | Unsupported DSH versions and object prototype properties (`toString`, `__proto__`, `constructor`) are rejected fail-closed; missing `webSocketTransport` withholds `web.routes`. | PASS |
| **S7-F10**| DSH Process Loss Behind Live RouteIngress | Node loss returns 502/504 and marks `reachable=unreachable`; automatic recovery restores `reachable=ok`; no cross-node crosstalk. Documented with redacted site-specific IP in `dsh-loss-recovery.json`. | PASS |
| **S7-F11**| Delete / Bookmark / Re-enrollment | Deleted node revokes hub route keys; stale bookmark fails closed (404); re-enrollment provisions fresh cryptographic route keys while preserving `same nodeId`. Documented in `delete-reenroll.json`. | PASS |
| **S7-F12**| Dropped Connection Cleanup | Early aborts and malformed requests release WebSocket and HTTP tracker counters on both Node RouteIngress and Hub proxy. | PASS |
| **S7-F13**| Process Restart Stability | Hub, Node, and Gateway restarts under active traffic recover cleanly without stale lock corruption or cross-session leakage. Documented in `restart-stability.json`. | PASS |

---

## 4. Evidence Artifacts Summary

All evidence artifacts are recorded in `test/evidence/stage7/` and pass Public Tree validation:

- `automated-drill.json`: Automated operational drill output (`runId: stage7-20260907020800145-37232`, `failedPredicates: []`, boolean secret protection preserved).
- `dsh-loss-recovery.json`: Physical NAS host A DSH process kill and live route recovery chronology (with IP redaction).
- `route-key-rotation.json`: Hub route key rotation overlap with authentic 32-hex `keyId`s, Hub restart during overlap, and scheduled revocation.
- `tls-failure.json`: TLS trust matrix verification using exact leaf certificate identity matching Stage 6 Rehearsal CA (`CN=stage6.localhost`).
- `delete-reenroll.json`: Operator delete tombstone, key revocation, 404 bookmark fail-closed, and fresh re-enrollment preserving the same `nodeId`.
- `restart-stability.json`: Active traffic concurrency during Hub, Node, and Gateway restarts with WS tracker verification.
- `live-two-host.json`: Full physical two-host drill summary, timestamped chronology conforming strictly to `C7.1 < Rehearsal < E7.2`, and sanitized topology.
- `manifest.json`: Artifact SHA-256 digests and formal binding to candidate commit `2559a17ed6e7ff0cbe58f1b45d40e3b166eb0582`.

---

## 5. Scope Boundary Compliance

No capabilities beyond Stage 7 hardening were introduced:
- No v0.5 reverse connection or NAT traversal.
- No fleet execution, `terminal.pty`, or `agents.run`.
- Package version preserved at `0.3.0` (Stage 8 release promotion has NOT started).

---

## 6. Review Gate Status

- **Stage 7**: **COMPLETE — awaiting independent Stage 7 review**
- **Stage 8**: **NOT STARTED**
Work is halted awaiting independent Review Gate audit before any Stage 8 activities begin.
