# v0.4 Stage 7 failure, restart, and compatibility hardening

Status: **READY_FOR_INDEPENDENT_STAGE7_FINAL_REVIEW**

This is the C7.2 evidence closure prepared for E7.3. It does not create a tag,
release, promotion, DNS cutover, or Stage 8 work. E7.3 is the evidence-only
seal child of C7.2.

## Provenance and chronology

| Field | Value |
|---|---|
| Branch | `chore/v0.4-stage7-corrective-evidence` |
| Stage 6 baseline | `e8bb6a6538439ed7a89b98e7812a02dda0468759` |
| C7.2 candidate | `190b296352513b1d92d359fefebf0f40569675aa` |
| C7.2 parent | `2559a17ed6e7ff0cbe58f1b45d40e3b166eb0582` |
| C7.2 commit time | `2026-09-08T11:16:54+08:00` |
| Physical rehearsal | isolated NAS-class + workstation-class two-host topology |
| Evidence execution | `2026-09-08` |
| E7.3 | evidence-only child to be created from C7.2 |
| Review gate | **READY_FOR_INDEPENDENT_STAGE7_FINAL_REVIEW** |
| Stage 8 | **NOT STARTED** |

No accepted Stage 0–7 history was amended, rebased, squashed, or overwritten.

## External DSH identity

The Real DSH acceptance used the exact pinned baseline:

```text
version: 0.1.1-rc.2
commit:  b150a551b8d465e31e418e1b2eaf5e79bbb7d28e
CLI SHA256: c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62
```

Real DSH result: **1 pass / 0 fail / 0 skip**. No TLS bypass was used.

## Automated verification

| Gate | Result |
|---|---:|
| Focused Stage 7 tests | 35 total / 33 pass / 0 fail / 2 skip |
| `npm run stage7:drill` | exit 0; `success=true`; `failedPredicates=[]` |
| Full `npm run check` | 385 total / 380 pass / 0 fail / 5 skip |
| Real DSH acceptance | 1 pass / 0 fail / 0 skip |
| Public Tree | PASS |
| `git diff --check e8bb6a6538439ed7a89b98e7812a02dda0468759..C7.2` | PASS |

The Windows skips are environment-specific POSIX permission checks and the
known GNU-tar Windows-path skip. They are not converted into PASS claims.

The runner-owned raw drill is `data/stage7-drill-evidence.json`; its sanitized
closure is `test/evidence/stage7/automated-drill.json`.

## Physical two-host evidence

The physical rehearsal used explicit CA trust, matching SAN validation,
`rejectUnauthorized=true`, and no certificate bypass.

Completed and recorded:

- Hub restart preserved both node IDs, route targets, route-key identities, and eligibility.
- Stopping only Node A's DSH behind a live RouteIngress changed A to `reachable=unreachable`; Node B remained `reachable=ok`.
- Restoring A's DSH and its isolated internal gateway produced a signed RouteIngress `200 ready=true`; A returned to `fresh/ok/pass/reachable=ok`.
- Hub route-key rotation produced exactly two keys during overlap; the old key was `rotating`, the new key was `active`, `revoke_after` survived Hub restart, and no third key appeared.
- Unknown CA was rejected, wrong SAN was rejected, and matching CA/SAN was accepted.
- Delete tombstoned Node A, revoked its old Hub route identities, and failed closed to `reachable=unknown`.
- Reenrollment restored the same node ID with a fresh Hub route key; Node B remained isolated and healthy.
- Corrected Node B physical DSH report passed patch, runtime, settings, authorization 6/6, session resume, plugin route, and WebSocket 101/Ping/Pong checks.

Supplementary physical observations still **NOT_EXECUTED** and explicitly
non-blocking:

- waiting for the real 14-day physical overlap window to elapse and observing scheduled old-key revocation. Deterministic automated S7-F6 revocation semantics passed.
- an independent physical abort-counter measurement for HTTP/WS cleanup. Automated S7-F12 and the stronger Stage 4 capacity/cleanup integration passed.

These observations remain transparent supplemental evidence and do not block
Stage 7 sealing.

## S7-F1 through S7-F13 matrix

| Predicate | Evidence | Status |
|---|---|---|
| S7-F1 migration v3→v4→v5 | automated-drill | PASS |
| S7-F2 backup/restore route identity | automated-drill | PASS |
| S7-F3 secret-bearing Registry permissions/scrubbing | automated-drill | PASS |
| S7-F4 private-key exclusion | automated-drill | PASS |
| S7-F5 corrupt persistence fail-closed | automated-drill | PASS |
| S7-F6 Hub route-key overlap/restart | automated-drill + route-key-rotation | PASS; scheduled physical revocation NOT_EXECUTED / NON-BLOCKING |
| S7-F7 process-local nonce replay | automated-drill | PASS; process-local bounded window only |
| S7-F8 TLS unknown-CA/wrong-SAN/matching matrix | tls-failure | PASS |
| S7-F9 unsupported DSH/capability withdrawal | automated-drill | PASS |
| S7-F10 DSH loss behind live RouteIngress | dsh-loss-recovery | PASS |
| S7-F11 delete/bookmark/reenroll | delete-reenroll | PASS |
| S7-F12 HTTP/WS cleanup | automated-drill | PASS; independent physical abort-counter NOT_EXECUTED / NON-BLOCKING |
| S7-F13 Hub/Node/Gateway restart stability | live-two-host + restart-stability | PASS |

Nonce replay protection is explicitly **process-local** and bounded by the
retention window. It is not durable replay protection across process restart.

## Evidence manifest

The evidence manifest is `test/evidence/stage7/manifest.json`. Every listed
artifact is bound to C7.2 and has a computed SHA-256 and byte count. The public
artifacts contain no private keys, token values or digests, passwords, cookies,
CSRF values, gateway secrets, or raw authenticated headers. Physical addresses
are redacted to stable host labels.

## Stop boundary

Stage 7 evidence is **ACCEPTABLE — GO TO SEAL E7.3**. The two transparent
supplementary physical observations remain `NOT_EXECUTED / NON-BLOCKING` and do
not alter S7-F6, S7-F12, or S7-F13 gate status. E7.3 must remain evidence-only,
with parent C7.2, and must be independently provenance-reviewed after push.
Do not tag, release, publish, promote, cut over DNS/gateway, or start Stage 8.
