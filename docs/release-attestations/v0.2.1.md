# Release attestation — v0.2.1

Sanitized record of the production rehearsal and promotion performed for this release. Host names, addresses, credentials, and site-specific paths are intentionally withheld; the downstream deployment retains the full evidence in its private state (rehearsal run directory and deployment record).

## What was rehearsed

- Rehearsed Orbit revision: `1e835dd3f45b95c57340a0285988c9c5dceadc7d` (review fixes for the upgrade guard).
- Rehearsal pattern: isolated compose project on the downstream production host — candidate DSH and gateway containers, candidate-only credentials and self-signed certificate, candidate data extracted from the validated baseline backup, docker network alias for the candidate endpoint, and the runner executed in a container on the candidate network.
- The production stack ran untouched throughout the rehearsal.

## Candidate workflow result

The full `candidate` workflow completed with `CANDIDATE PASSED - ELIGIBLE FOR MANUAL PROMOTION` (exit code 0):

| Check | Result |
| --- | --- |
| globalPatch | pass — exact matchers verified in the candidate container |
| profilePatch | pass — profile-local copy verified |
| runtimeReadiness | pass — authenticated gateway request returned 200 |
| settingsRead | pass — settings.describe succeeded |
| settingsNoopWrite | pass — no-op settings.mutate succeeded |
| authorizationSmoke | pass — 6/6 cases matched (positive control, unauthenticated, invalid credentials, unexpected Origin, Sec-Fetch-Site cross-site, forged identity assertion) |
| sessionResume | pass — a session created before the rehearsal resumed on the copied candidate data and re-selected its current model |
| webPluginRoutes | pass — lazy-loaded plugin asset served through the candidate gateway |
| longLivedTransport / terminalPtty | not_run — no automated checks in this release (recorded explicitly) |

Binding evidence recorded before start: the resolved compose configuration matched the candidate specification (image, `/data` and `/workspace` mounts, published loopback port, project name), and the running stack carried the per-run candidate token.

## Snapshot and rollback references

- A production data snapshot was taken before any promotion activity via the snapshot contract, and its manifest was bound to the request tuple (id, data root, revisions) and timestamp window.
- Rollback targets at promotion time: the previous production image (pinned by digest in the downstream deployment record) and the pre-promotion data snapshot archive.

## Promotion and post-promotion verification

Promotion was an explicit operator action (approved after report review). The production stack was recreated on the candidate image (pinned by digest), and the following checks were re-run through the production gateway, all passing:

- startup patch verification `ok/ok` for the global and profile-local copies;
- runtime hooks compatible;
- settings read and no-op write;
- existing-session resume with current-model re-selection;
- 6/6 live authorization smoke cases.

## Release identity

- The release commit contains only documentation, version metadata, and a test fixture rename; the image-relevant paths (`src/`, `bin/`, `docker/`) are identical to the rehearsed revision.
- The compatibility report schema is version 2: `compatibility` and `promotion readiness` are reported separately, and promotion eligibility requires the exact candidate identity, the exact baseline rollback target, and a completed snapshot.
