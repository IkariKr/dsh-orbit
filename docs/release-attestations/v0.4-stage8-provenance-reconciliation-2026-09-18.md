# v0.4 Stage 8 Provenance Reconciliation

## Disposition

```text
Provenance reconciliation: HOLD
Stage 8 candidate: NOT SELECTED
Stage 8 construction: NOT AUTHORIZED
Tag/release/promotion authorization: NOT ESTABLISHED
Runtime changes in this reconciliation: NONE
```

This report is a docs-only audit record. It does not select an existing Stage 8
branch, repair a historical tag, create a release, or authorize runtime work.
All historical refs and objects are retained unchanged.

A later, separately reviewed construction authorization now exists:
`S8-CONSTRUCTION-20260918-A1` in
`docs/release-attestations/v0.4-stage8-construction-authorization-2026-09-18.{md,json}`.
The HOLD block above is retained as the historical pre-authorization state. The
new authorization supersedes only the "construction not authorized" stop; it
does not resolve or rewrite the historical E8.x tag/release contradictions, and
it does not authorize tag/release, canonical E9, P3, or production promotion.

## Accepted reconciliation base

The intended future Stage 8 base is the accepted Stage 7 remote tip:

```text
de8eb467a844ef56191f629cdecc6879e561d90a
```

No existing Stage 8 ref is a descendant of this commit. Existing Stage 8 lines
therefore cannot inherit the Stage 7 acceptance, its chronology, or its
provenance authorization by ancestry implication. A future valid candidate must
be a new descendant of `de8eb467` created under a separate Stage 8
construction authorization.

The repository copy of the older Stage 7 construction attestation still
contains the historical wording `Stage 7 acceptance: HOLD`, while the current
independent review disposition accepts `de8eb467` as the Stage 7 base. This
wording discrepancy is disclosed rather than silently rewritten by this Stage 8
audit. It must not be used to infer Stage 8 authorization; the new Stage 8
candidate and its authorization must be explicit.

## Competing historical lines

### Root E8.3 line

```text
b8bdbbd...  E7.2 evidence baseline
  └─ 90312b6...  tested candidate
      └─ 56fde22...  E8.3 closure
```

The E8.3 manifest records a blocked/not-executed mounted gate because the
current-run browser lifecycle checkpoint and node identity evidence were
missing. It is a historical HOLD, not a mounted PASS.

### E7.3/E8.5 line

```text
0dc00ce...  E7.3 evidence seal
  └─ a5709c2...  construction transplant
      └─ 5b1856b...  mounted lifecycle construction
          └─ 8f17cf7...  provenance gate
              └─ ... ── c1ace3a...
```

The E8.5 closure object is:

```text
candidate: a263e7592d0726dd3c30895e27469a30f94b1768
closure:   89204e026c5478f6591525eb1c6e5d364cbeb857
runId:      634cda14-8291-46af-8b4f-4316c83f1331
```

Its mounted evidence is the most complete historical Stage 8 record, but it is
bound to the old candidate and old E7.3 parent. It cannot be rebound to a future
candidate by editing JSON or copying hashes.

The authority-boundary tip `7b10435` retains inherited E8.5 evidence but is not
a new evidence closure for the later code in that branch.

### Competing E8.4/E8.2 lines

```text
4e587b1  E8.4 mounted PASS, old E7.2 parent, no later raw-evidence closure
82e173b  E8.2/C8.2.4 side branch, blocked Docker engine history
```

They remain historical inputs only. Neither is a valid base for a new
`de8eb467`-descendant candidate.

## Tag and public-release contradiction

The repository contains an annotated tag:

```text
v0.4.0-rc.1
Tag object: 95440e3d0d836e707b2f836dfe71dd4bde49dcf1
Peeled commit: 89204e026c5478f6591525eb1c6e5d364cbeb857
```

However, the attestation on the tagged closure still states:

```text
Final Review: NOT YET PERFORMED
Tag: NOT CREATED
Publication: NOT PUBLISHED
Production promotion: NOT EXECUTED
No authorization granted
```

The history also contains a merge-to-main/public wording line that claims a
release state while the attestation and SOP retain the pre-final-review state.
No independently verifiable reviewer approval record has been found in the
audited Git/GitHub history. GitHub metadata confirms that a historical release
action did occur: the annotated tag and prerelease `v0.4.0-rc.1` are published,
and its release body claims `E8.5 Final Review: PASS`, `tag: created`, and
`published: true`. However, the tagged attestation still states `Final Review:
NOT YET PERFORMED`, `Tag: NOT CREATED`, and no authorization granted; PR #6 is
merged but has an empty `reviewDecision` in the available metadata. The formal
resolution is therefore: historical release action exists, but its authorization
provenance is contradictory/not independently verifiable. The tag and release
are retained immutably; no retroactive authorization is inferred, and they do
not authorize a new Stage 8 candidate.

This is an unresolved historical provenance contradiction. The tag is retained;
it is not deleted, moved, or treated as proof of authorization. Mainline/public
wording is not used to override the contradictory attestation or SOP gate.
The merge/public line is recorded explicitly: merge `3b9a28f` brought the
authority line into main, and `7a8bdf1` states that the pre-release final review
passed, while the tagged attestation still states that Final Review was not
performed and no authorization was granted. This report does not choose between
those contradictory claims; it records the authorization gap.

## Evidence reuse classification

### Historical design/input only

The following may inform a future authorized construction:

- authority-boundary implementation patterns;
- mounted browser lifecycle scenarios;
- the 25-field mounted matrix;
- runner raw-to-emitted evidence binding rules;
- release-contract assertions;
- promotion-plan and rollback structure.

### Must be rebuilt for a new candidate

For any candidate different from the historical exact SHA, regenerate and
re-execute all candidate-bound material:

```text
fresh-install.json
migration.json
backup-restore.json
two-node-mounted-smoke.json
mounted-runner-raw.json
promotion-plan-validation.json
manifest.json
release attestation
closure hashes
```

Do not rewrite an old candidate SHA, reuse an old run ID or node IDs, copy old
raw evidence, or inherit a historical mounted PASS.

### Quarantined historical material

The following remain preserved but are not current candidate or authorization
evidence:

```text
chore/v0.4-stage8-release-closure       @ 56fde22
chore/v0.4-stage8-e8.4-mounted-pass    @ 4e587b1
chore/v0.4-stage8-c8.2.4               @ 82e173b
chore/v0.4-stage8-release-closure-e7.3 @ c1ace3a
chore/v0.4-stage8-authority-boundary-c8.4 @ 7b10435
v0.4.0-rc.1                            @ 89204e0
```

Unreferenced/dangling Stage 8-like objects and superseded stash drafts are
also audit history only. None is deleted or rewritten by this reconciliation.

## Stop conditions

The following actions remain prohibited until the contradiction is resolved by
an independent authorization and a new Stage 8 construction order:

```text
Docker Compose or mounted browser execution
evidence emitter or runtime drill
canonical E9
P3
production promotion
tag/release mutation
```

The only admissible next step is a separately authorized Stage 8 construction
from `de8eb467`, with a new exact candidate, fresh evidence, an evidence-only
closure child, and a new independent Final Review. The historical E8.5 material
is design/input only; every candidate-bound artifact listed above must be rebuilt
for the new candidate. Until then:

```text
Stage 8 provenance reconciliation: HOLD
Stage 8 candidate: NOT SELECTED
Stage 8 accepted status: NOT ESTABLISHED
v0.5 runtime implementation: NOT AUTHORIZED
```
