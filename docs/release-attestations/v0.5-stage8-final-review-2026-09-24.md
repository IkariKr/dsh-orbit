# v0.5 Stage 8 Independent Final Review — Closure Record

Status: **FINAL REVIEW PASS — v0.5 STAGE 8 CLOSED; v0.5 ENGINEERING ACCEPTANCE = PASS**

Date: 2026-09-24

## Review input

- Accepted construction candidate:
  `7e8bb397775dc37f411cc084173525fbb056bf15`
  ("fix(v0.5): probe reverse readiness at DSH endpoint"), branch
  `chore/v0.5-stage2-public-machine-ingress`.
- Accepted evidence-only closure (direct child of the candidate):
  `bfcc541d84f3fc5fb3bb14fa54100276e41816ba`
  ("docs(release): record Stage 8 mounted final evidence for v0.5.0-rc.1").
  The closure commit is pushed; local and remote SHA are an exact match with
  0/0 divergence on `origin/chore/v0.5-stage8-release-closure`.
- Attestation: `docs/release-attestations/v0.5.0-rc.1.md` (evidence phase
  Stage 8; mounted runId `v05-formal-1790252625`; 48/48 matrix PASS:
  15 automated qualification + 33 live mounted).
- Reviewer: independent code reviewer process (Gate 4 Final Review, report
  `dsh-orbit-v05-stage1/docs/review/2026-09-24-v05-stage8-release-closure-final-rereview-r3-bfcc541.md`,
  Verdict PASS WITH NON-BLOCKING FINDINGS, P0 = 0, P1 = 0, P2 = 0, P3 = 1).

## Scope of the independent review (all mechanically re-verified)

1. Authorization lineage — `V05-CONSTRUCTION-20260919-A1`; ancestors
   `9891ab858a9c953a211978580910efcc2158bcd7` (accepted v0.4 closure),
   `30f8443329f1abeb7ad68d439424bfa56953a9e7` (governance predecessor),
   `402d8995d225346679d514edb4b42208a5c773cc` (Gate A Review GO), and
   `5738c0ce6a4ec11bee62f9cfae44dd6463816768` (RFC-0012 design baseline).
2. Candidate scope — diff confined to reverse readiness probing at direct DSH
   target endpoint; no forbidden feature creep or out-of-scope files.
3. Candidate tests — full suite re-run in a clean worktree at the candidate:
   497 pass, 0 fail, 6 skipped.
4. 48-field mounted matrix — raw, smoke, and attestation all exactly 48
   fields, all PASS (15 automated qualification + 33 live mounted).
5. Manifest hash/bytes — all seven artifacts re-hashed (LF bytes) and
   re-counted; every SHA-256 and byte count matches `manifest.json`; smoke
   `rawEvidenceSha256`/`rawEvidenceBytes` match the raw record;
   no closure self-reference.
6. runId / node IDs — `v05-formal-1790252625`,
   `node_06827d59b9d6511f05883387f431fbba` (Node A, direct),
   `node_62e2f94672739bc519887316c7bc3455` (Node B, reverse) consistent
   across raw, smoke, manifest, attestation.
7. DSH identity — `0.1.1-rc.2` /
   `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e` /
   `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`
   consistent everywhere.
8. Browser challenge — `031218b784a6b85e9a83cbd62d4bd9095a6f406cadd6f99055f2fd4e62adf9a1`
   mechanically derived by the runner (`sha256(BROWSER_CHALLENGE)`) and
   consistent in all bindings.
9. Browser producer — `runner-owned-firefox-selenium` everywhere.
10. TLS CA/leaf fingerprints —
    CA `4B:03:0F:54:F6:7A:50:A4:B7:65:2B:92:20:A6:DA:40:DD:0B:2C:F5:5E:CE:20:DB:1E:EF:44:BB:09:A9:6E:2C`,
    leaf `CD:AF:FE:F6:0B:6D:A6:E4:52:14:E0:2D:CD:B3:90:85:6F:37:A1:05:2D:A5:AD:E9:7D:CF:0A:CA:D1:C8:C2:5C`,
    consistent in raw, smoke, manifest, attestation.
11. Gateway binding — `https://127.0.0.1:8547` everywhere.
12. NSS trust mode — `firefox-profile-nss`; bridge verified to use NSS
    `certutil` only and to never touch Windows certificate stores.
13. Closure direct-child — `bfcc541d^ == 7e8bb397`, single parent.
14. Closure path purity — diff is exactly the attestation plus
    `test/evidence/v05/**` (8 files); `git diff --check` clean; worktree
    fully clean.
15. Closure full tests — focused v0.5 contracts 101/101 pass; stage8 release
    contracts 10/10 pass; `npm run check` 503 tests / 497 pass / 0 fail /
    6 skip; public-tree validation passed.
16. Residue hygiene — `data/**`/`secrets/**` ignored residue 0;
    `dsh-orbit-firefox-*` temporary profiles 0; `Cert:\CurrentUser\Root`
    and `Cert:\LocalMachine\Root` drill CA count 0; drill Docker containers
    0; runner/orphan processes 0.
17. Reverse-only proof — `inboundDeniedProbe` targeting Node B on port `9445`
    from Hub network namespace returned `connection-refused` (SHA-256
    `4a95397d2951ca011c7a982b5af4b79b0541a699e5c0e7571a3bc303894b5d68`).
18. Direct regression — Node A direct target `https://dsh-a:9444` intact and
    verified isolated.
19. Attestation discipline — no closure SHA self-reference; dispositions
    record Final Review pending at closure time.
20. Tag discipline — annotated tag `v0.5.0-rc.1` created on `bfcc541d` and
    pushed to origin; production promotion and DNS cutover remain separate.

## Findings note

- P0: 0
- P1: 0
- P2: 0 (prior rounds' Scope classification and citation paths resolved)
- P3: 1 (non-blocking) — `v0.5.0-rc.1` free-prose SHAs and paths are not
  mechanically validated by a release-contract test; deferred to post-closure
  harness maintenance as evidence-only closures cannot touch test code.

## Formal dispositions

- **v0.5 Stage 8 = CLOSED**
- **v0.5 engineering acceptance = PASS**
- Construction candidate: **PASS** (`7e8bb397775dc37f411cc084173525fbb056bf15`)
- Mounted final evidence: **PASS** (48/48, runId `v05-formal-1790252625`)
- Independent Stage 8 Final Review: **PASS** (P0 = 0, P1 = 0, P2 = 0)
- Release tag: `v0.5.0-rc.1` (annotated tag on `bfcc541d`, pushed to origin).

These dispositions separate engineering acceptance from the following,
each of which remains **independently authorized and not yet authorized**:

- Production promotion — separate authorization; **not authorized, not performed**.
- DNS cutover — separate authorization; **not authorized, not performed**.

v0.6 construction is **not** gated on production promotion or DNS cutover.

## v0.6 baseline designation

The **only** lawful starting point for v0.6 work is the accepted v0.5 closure:

```text
v0.6 baseline (accepted v0.5 closure):
bfcc541d84f3fc5fb3bb14fa54100276e41816ba
```

v0.6 work must not fork from:

- any historical Stage 8 branch or worktree;
- any superseded or unaccepted closure attempts;
- any pre-final-review candidate;
- any experimental worktree.

Everything else in the repository history is either accepted ancestry of
this baseline or quarantined historical construction.
