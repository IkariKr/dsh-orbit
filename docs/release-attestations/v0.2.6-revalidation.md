# Revalidation attestation — v0.2.6 exact tag (frozen baseline record)

This attestation closes the v0.2 Upgrade Guard frozen baseline by revalidating the exact `v0.2.6` tag object against the downstream records and the repository evidence. It is a records-only revalidation of the already-released tag; no code, image, or deployment state was changed by it.

Scope note: this is a v0.2 historical record. It truthfully mentions the legacy third-party (dsh-ssh) items that were part of that frozen baseline; no v0.3 design, code, schema, capability, or test introduces or extends third-party plugin references (ADR-0001).

## Exact tag identity

- Tag: `v0.2.6` (annotated), commit `afd8de9b0a76aa7470cef2ca769581bfc2dc278a`.
- Rehearsed code commit: `044afbae0f6bd2b4613e75d1a6afe2ac2e0b5c27` (the candidate/promotion ran against this).
- Release delta over the rehearsed commit: `git diff --stat 044afba..afd8de9 -- src bin docker` is **empty** — the release commit changed only documentation, version metadata, and changelog; the image-relevant paths are byte-identical to the rehearsed state.
- Remote refs verified: `main` and `v0.2.6` pushed; upstream tags `v0.2.0`–`v0.2.5` untouched.

## Downstream deployment record (frozen baseline)

- Production image: `dsh-orbit:0.1.1-rc.2-ikari.11` (digest `sha256:ad3a966f19b273c9f918e074111e92c3aaeaca66960701f447438d4e9f71dfc5`), built from the rehearsed commit; `DSH_ORBIT_REF=044afbae…`, `DSH_ORBIT_PATCH_DSH_SSH=1`, `DSH_SSH_PLUGIN_VERSION=0.3.2` in the deployment record.
- Post-promotion evidence on the production gateway (all passing): patch verification `ok/ok`, strict dsh-ssh bundle verification `ok`, settings read/write, historical-session resume, authorization smoke 6/6, terminal fence smoke 6/6.
- Rollback targets: previous image `dsh-orbit:0.1.1-rc.2-ikari.10` (digest `sha256:10aebdd7…`, still present) and data snapshot `pre-candidate-2026-08-30T03-51-03-464Z.tar.gz` (manifest bound to the request tuple, restore reference in the deployment record).

## Frozen-baseline statement

The v0.2 Upgrade Guard milestone is frozen on the exact `v0.2.6` tag (commit `afd8de9b…`): source identity (tag+commit), image identity (digest, content-identical to the rehearsed commit), data recovery point (snapshot + manifest), and rollback targets are all recorded and reproducible. Subsequent work on v0.3 must not move, retag, or silently amend this baseline.