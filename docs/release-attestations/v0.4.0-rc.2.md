# v0.4.0-rc.2 Release Attestation — Stage 8 Mounted Final Evidence (E8.6)

Status: **mounted final evidence recorded; awaiting independent Stage 8 Final Review**

Date: 2026-09-19

This attestation is the evidence-only closure of Stage 8 construction under
authorization `S8-CONSTRUCTION-20260918-A1`. It records one fresh,
candidate-bound mounted final run and the mechanically emitted evidence
artifacts. This file and `test/evidence/stage8/**` are the only paths changed
by this closure commit, which is a direct child of the frozen candidate.
Per the closure rules, this attestation must not reference its own Git SHA;
the closure commit SHA is recorded externally after the commit is created.

## Authorization and lineage

- Construction authorization: `S8-CONSTRUCTION-20260918-A1`
  (`AUTHORIZED_FOR_STAGE8_CONSTRUCTION`), recorded in the pushed commit
  `6f766acfc67dff41afe15f228078938f44922a87`.
- Accepted runtime base: `de8eb467a844ef56191f629cdecc6879e561d90a`.
- Governance predecessor: `2a1f41a734e0395615ab0ae1ee41173e257a42ec`.
- Frozen construction candidate (direct child of the authorization root
  lineage, candidate scope allowlist verified by
  `test/stage8-release-contract.test.mjs`):
  `4c6b03319de80e04a3e222a3121768cf58b0c8b5`
  ("fix(stage8): isolate Firefox trust to profile").
- Candidate worktree at evidence execution: clean; local/remote SHA exact
  match on `origin/chore/v0.4-stage8-construction-s8-construction-20260918-a1`.

## External harness baseline (DSH)

- DSH version: `0.1.1-rc.2` (installed inside the drill node images from
  `docker/Dockerfile`, `npm install -g @deepseek-ai/dsh@0.1.1-rc.2`).
- DSH upstream commit binding: `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`.
- DSH CLI binary (`lib/bin.js`) SHA-256:
  `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`,
  measured directly inside both drill images (`dsh-orbit:dsh-drill-a` and
  `dsh-orbit:dsh-drill-b`) during this evidence run; the value matches the
  previously reviewed historical binding for `0.1.1-rc.2`.

## Mounted final run (unique, fresh, candidate-bound)

- runId: `a593d46b-4ccd-43fc-a8c4-9d5f52365756`
- Runner: `scripts/registry-drill.mjs --compose-up --wait-for-browser`
  (producer `registry-drill-runner`, raw kind
  `stage8-mounted-runner-raw`).
- Candidate commit bound in raw evidence:
  `4c6b03319de80e04a3e222a3121768cf58b0c8b5`.
- Execution window: `2026-09-19T13:51:23.908Z` →
  `2026-09-19T13:54:55.279Z` (UTC).
- Node IDs (fresh, current-run):
  - A: `node_cf6f53f382a7003f4f3e38a0ae5fecfe`
  - B: `node_1882c4d5d8b0d25be53c0964459cb21b`
- Browser producer: `runner-owned-firefox-selenium` (runner-owned temporary
  profile; browser challenge digest
  `1824a3bf4301b6e9921e3708b86df9d92663221a2d34513d5cc76c1ba3854457`).
- TLS trust: `trustMode = firefox-profile-nss`; TLS validation
  `enabled`; `accept_insecure_certs = false`;
  `security.enterprise_roots.enabled = false`. The run-scoped drill CA was
  imported only into the run-owned temporary Firefox profile NSS database
  (`-N --empty-password`, `-A -t C,,`, verified by `-L` after import).
  - CA (CN=dsh-orbit-drill-ca) SHA-256 fingerprint:
    `83:D5:E8:BB:C3:20:23:71:86:C2:E7:27:F1:F7:6B:B2:58:E2:C4:D6:A9:2B:35:1C:93:30:A1:59:ED:82:07:80`
  - Leaf SHA-256 fingerprint:
    `A0:E8:77:2E:42:00:08:A6:B0:4C:BB:18:F6:3E:36:E1:18:0E:48:E6:BC:3D:F4:62:0D:CB:BB:5B:29:5D:DA:8E`
  - Leaf SANs: `127.0.0.1, registry-hub, dsh-orbit.test, *.dsh-orbit.test,
    dsh-a, dsh-b, dsh-a.test, dsh-b.test`.
- DSH binding recorded in raw evidence: version `0.1.1-rc.2`, commit
  `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`, CLI SHA-256
  `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`.
- Contact aging: controlled accelerated contact aging for the drill only
  (clock file under run-owned residue); production heartbeat thresholds
  unchanged (cadence 60s, stale after 3 missed beats, lost after 24h).

## Required mounted matrix — 25/25 PASS

| # | Field | Result |
|---|-------|--------|
| 1 | routeTargetsConfiguredAB | PASS |
| 2 | routeTargetsPersisted | PASS |
| 3 | eligibilityAB | PASS |
| 4 | selectorListsAB | PASS |
| 5 | selectorOpenA | PASS |
| 6 | selectorOpenB | PASS |
| 7 | httpRootA | PASS |
| 8 | httpRootB | PASS |
| 9 | staticAssetA | PASS |
| 10 | staticAssetB | PASS |
| 11 | websocketUpgradeA | PASS |
| 12 | websocketUpgradeB | PASS |
| 13 | websocketPingPongA | PASS |
| 14 | websocketPingPongB | PASS |
| 15 | cookieIsolation | PASS |
| 16 | nodeContextIsolation | PASS |
| 17 | gatewayRestartRecovery | PASS |
| 18 | hubRestartRecovery | PASS |
| 19 | nodeAFailClosedOutage | PASS |
| 20 | nodeBHealthyDuringAOutage | PASS |
| 21 | dshLossAndRecovery | PASS |
| 22 | bookmarkFailClosed | PASS |
| 23 | sameNodeIdReenroll | PASS |
| 24 | freshHubRouteIdentity | PASS |
| 25 | deleteBookmarkAndReenroll | PASS |

The matrix values are produced only by the runner and validated by
`scripts/emit-stage8-mounted-evidence.mjs` (`requirePass` shape check);
no PASS value was accepted from CLI input.

## Fresh evidence artifacts (SHA-256 / bytes)

All artifacts were produced during this evidence phase for candidate
`4c6b03319de80e04a3e222a3121768cf58b0c8b5`; no historical artifact was
rebound. `manifest.json` carries the full binding set and the SHA-256 and
byte counts below.

All hashes bind the exact committed artifact bytes (LF; the repository
`.gitattributes` enforces `eol=lf`), so a reviewer can verify each artifact
directly from the closure commit's blobs.

| Artifact | SHA-256 | Bytes |
|----------|---------|-------|
| fresh-install.json | `f7fca36805597cd651b7474e232283bb986e9c6423cdb5f0b148e9838cf4619b` | 624 |
| migration.json | `af827da7b799e478d0b11dc7c912d3a1bca182101edd489a423845ecd1480816` | 671 |
| backup-restore.json | `39d3c3a71c581a1613998b4faac9cf9b2a78362f307acddd9f2ca53fddd437cc` | 741 |
| mounted-runner-raw.json | `17fbd34e03932ed1bd079d53e4f8a12a18faa85922ba4fd510496e3f03b29304` | 8455 |
| two-node-mounted-smoke.json | `17e2f4346f9b7b0e0c5d99836be11ed7e25a11fa0584f4a43ead5b150e3ef9de` | 4312 |
| promotion-plan-validation.json | `4c496871f80a578dded7996c7279febca7ed3e41f5e7057fed4742897723a0ec` | 540 |

- `mounted-runner-raw.json` is the verbatim runner raw record copied by the
  emitter from run-owned residue; `two-node-mounted-smoke.json` is the
  emitter output (schemaVersion 3) and binds `rawEvidenceSha256` /
  `rawEvidenceBytes` of the raw record.
- `fresh-install.json`, `migration.json`, and `backup-restore.json` record
  fresh executions of the local-file-backed acceptance test commands on the
  candidate worktree (43 tests / 41 pass / 0 fail / 2 skip; 25 tests /
  25 pass / 0 fail; 28 tests / 26 pass / 0 fail / 2 skip).
- `promotion-plan-validation.json` records that the production promotion and
  rollback plan is documented and reviewed while promotion execution itself
  remains deferred and separately authorized.

## Run residue hygiene after the mounted run

- `data/drill-evidence.json` was removed by the evidence emitter
  (`rawSourceRemoved: true`).
- Run-owned residue (`data/orbit-drill/**`, drill proxy secret under
  `secrets/`) was removed by bounded run-owned cleanup; the drill recorded
  cleanup as complete and both node daemons stopped and the compose stack
  down.
- Ignored runtime residue check after the run:
  `git ls-files --others --ignored --exclude-standard -- data secrets` →
  empty (0 entries).
- The run-owned temporary Firefox profile and its NSS databases were deleted
  by the bridge lifecycle; no `dsh-orbit-firefox-*` temporary profile remains.
- Windows certificate stores after the run:
  `Cert:\CurrentUser\Root` CN=dsh-orbit-drill-ca = 0 and
  `Cert:\LocalMachine\Root` CN=dsh-orbit-drill-ca = 0. No OS Root
  trust-store mutation occurred (forbidden by
  `docs/sop/verification-tls-trust-policy.md` and removed from the harness).

### NSS certutil environment note (outside the repository)

The machine provides Mozilla NSS `certutil` (MSYS2 mingw-w64 build of NSS
3.129 with its NSPR/SQLite/Zlib runtime DLLs) under
`C:\Users\41382\tools\nss\mingw64\bin\`, exposed to the harness through
`DSH_ORBIT_NSS_CERTUTIL=C:\Users\41382\tools\nss\nss-certutil.cmd`. That
launcher (a) passes every invocation through unchanged to the real NSS
`certutil.exe` beside it, and (b) answers the harness capability probe
(`certutil -H`) by running the same real binary's `--syntax` summary,
because recent NSS builds answer `-H` with a detailed help format that does
not carry the `certutil -A` / `certutil -N` usage literals the probe checks
for. All database operations (`-N`, `-A`, `-L`) are executed by the genuine
NSS binary; the Windows built-in `certutil.exe` was verified to be rejected
by the probe (negative test executed during environment preparation).

## Verification scope note for the closure tree

`test/stage8-release-contract.test.mjs` ("Stage 8 construction root and
candidate boundary are mechanically anchored") enforces the candidate scope
allowlist over `git diff --name-only 6f766acf..HEAD`. At this closure commit
that diff necessarily includes `test/evidence/stage8/**`, which the candidate
allowlist deliberately excludes (`candidateForbiddenPaths`), so exactly this
one test fails when the full check is executed in the closure tree, while
passing at the candidate itself (16/16 contract tests verified in a clean
worktree at `4c6b03319de80e04a3e222a3121768cf58b0c8b5`). The closure's own
path purity is governed by the authorization's closure diff gate
(`candidateToClosure.allowedPaths`) and is satisfied by this commit: its
entire diff is `docs/release-attestations/v0.4.0-rc.2.md` plus
`test/evidence/stage8/**`. Full check in the closure tree: 392 tests /
386 pass / 1 fail (the scope artifact above) / 5 skip, reproduced across
repeated runs; one intermittent unrelated test failure was observed in a
single earlier full-suite run and did not reproduce.

## Dispositions

- Construction candidate: **PASS** (`4c6b03319de80e04a3e222a3121768cf58b0c8b5`)
- Mounted final evidence: **PASS** (25/25; this closure, E8.6)
- Independent Stage 8 Final Review: **NOT YET PERFORMED** (awaiting)
- Tag / release creation: **NOT AUTHORIZED / NOT PERFORMED** (`not-created`,
  `false`)
- Production promotion: **NOT AUTHORIZED / NOT PERFORMED** (`not-performed`)
- This closure records evidence only; tag/release and production promotion
  each require separate explicit authorization.
