# v0.4 Stage 7 Construction Report

## Disposition

```text
Stage 7 implementation: COMPLETE
Stage 7 evidence: SEALED
Independent Stage 7 final review: PENDING
Stage 7 acceptance: HOLD
Stage 8: NOT STARTED
v0.5 runtime implementation: NOT AUTHORIZED
```

This is an evidence-backed construction report, not an independent approval.
It records the final C7.2/E7.3 candidate and stops at the independent review
boundary. It does not authorize tagging, release publication, production
promotion, Stage 8 construction, or v0.5 runtime work.

## Exact provenance

| Field | Value |
|---|---|
| Stage 6 baseline | `e8bb6a6538439ed7a89b98e7812a02dda0468759` |
| C7 candidate | `190b296352513b1d92d359fefebf0f40569675aa` |
| E7.3 evidence seal | `0dc00ceb3b0574e2a6bd81eb62502fd6c2e233f3` |
| Branch | `chore/v0.4-stage7-corrective-evidence` |
| Working tree at report time | clean |
| Report verification time | `2026-09-18T02:38:00Z` |
| Stage 7 evidence manifest | `test/evidence/stage7/manifest.json` |
| Stage 7 attestation | `docs/release-attestations/v0.4-stage7-failure-hardening.md` |

The C7 candidate is a descendant of the accepted Stage 6 baseline. The E7.3
commit is an evidence-only child of the C7.2 candidate. No accepted Stage 6
history was amended, rebased, squashed, or overwritten.

## Hardening matrix

| Required predicate | Result |
|---|---|
| v0.3 -> v0.4 migration | PASS |
| Route/Hub identity backup and restore | PASS |
| Secret-bearing file protection and sanitized inspection | PASS |
| Route/key corruption fail-closed | PASS |
| Hub route-key rotation and restart overlap | PASS |
| Process-local nonce/replay semantics | PASS; bounded restart window explicitly retained |
| TLS unknown-CA, wrong-SAN, and matching-certificate matrix | PASS |
| Stale compatibility and capability withdrawal | PASS |
| DSH loss behind live RouteIngress and recovery | PASS |
| Delete/bookmark/reenroll identity lifecycle | PASS |
| HTTP/WS dropped-connection cleanup | PASS |
| Hub/Node/Gateway restart stability | PASS |
| Physical two-host failure rehearsal | PASS |

The automated drill records all required predicates as true:

```text
exitCode=0
success=true
failedPredicates=[]
```

## Verification evidence

The sealed verification summary records:

```text
Focused Stage 7 tests: 35 total / 33 pass / 0 fail / 2 skip
Full check: 385 total / 380 pass / 0 fail / 5 skip
Real DSH 0.1.1-rc.2: 1 pass / 0 fail / 0 skip
Public Tree: PASS
git diff --check: PASS
```

The current final-candidate checks additionally confirm:

```text
Stage 7 focused tests: 33 pass / 0 fail / 2 Windows permission skips
Evidence artifact SHA-256 and byte counts: PASS
Public-tree validation: PASS
git diff --check from Stage 6 baseline: PASS
```

The Windows permission checks are not converted into POSIX PASS claims. The
sealed evidence documents the platform limitation and the corresponding ACL
expectation. The known GNU-tar Windows-path skip is likewise retained as a
skip, not relabeled as a pass.

## Physical evidence and disclosed supplements

The sealed two-host evidence covers:

- Hub restart with node, route target, route-key, and eligibility preservation;
- DSH loss behind a live RouteIngress with Node B isolation;
- deterministic DSH recovery and eligibility restoration;
- route-key overlap and restart persistence without a third key;
- unknown-CA and wrong-SAN rejection with valid CA/SAN recovery;
- delete/tombstone and fresh-key reenrollment;
- restart stability and no cross-node confusion.

The following remain explicitly disclosed supplemental observations and are not
silently promoted to PASS:

- the real 14-day physical old-key scheduled-revocation observation;
- an independent physical HTTP/WS abort-counter measurement.

The deterministic automated predicates for route-key revocation and resource
cleanup passed. The sealed evidence classifies the two physical observations as
non-blocking supplements, subject to independent reviewer confirmation.

Nonce replay protection remains process-local. Restart resets the nonce cache;
the accepted bounded replay window relies on the timestamp-skew and single-Hub
assumptions. It is not durable replay prevention.

## Scope boundary

No reverse connection, NAT traversal, device pairing, fleet execution,
terminal.pty feature, new selector capability, new DSH compatibility profile,
or other v0.5/v0.6 feature was added in Stage 7.

The next v0.5 work permitted before implementation authorization is limited to
RFC/ADR drafting, threat modeling, lifecycle design, and test/evidence planning.
Runtime implementation requires a separate accepted v0.5 construction order
and independent architecture/security review.

## Stop point for independent review

The next action is independent Stage 7 final review of:

```text
C7 candidate: 190b296352513b1d92d359fefebf0f40569675aa
E7.3 seal:    0dc00ceb3b0574e2a6bd81eb62502fd6c2e233f3
```

Until that review explicitly accepts the candidate, the disposition remains:

```text
Stage 7: COMPLETE — awaiting independent review
Stage 8: NOT STARTED
v0.5 implementation: NOT AUTHORIZED
```
