# v0.3 Stage 7 operational hardening

Status: **in progress; awaiting required operational evidence**

This document records the Stage 7 operational-hardening candidate. It is
separate from the Stage 6 mounted Gate B attestation and does not authorize
Stage 8.

## Scope

The candidate covers only the SOP Stage 7 scope:

- fresh file-backed Registry installation and startup maintenance;
- real file-backed v1 → current, v2 → current, and v3 → current migration;
- SQLite-consistent backup → mutation → restore;
- fail-closed unsupported, malformed, and corrupt database handling;
- persistent-state inspection, integrity checks, and restart seams;
- operational retention and recovery evidence where recorded by the Stage 7 drill.

No wire protocol, authentication/trust model, Node identity semantics, schema
contract, routing, reverse connection, fleet, terminal, or third-party plugin
scope was added.

## Implementation

- SQLite schema validation and startup classification: `src/registry/sqlite.mjs`
- SQLite operational backup/restore: `src/registry/backup.mjs`
- Hub startup fail-closed boundary: `bin/dsh-orbit-hub.mjs`
- Isolated operational drill: `scripts/registry-stage7-drill.mjs`
- Regression coverage: `test/registry-backup.test.mjs`,
  `test/registry-stage7-contract.test.mjs`, `test/registry-sqlite.test.mjs`,
  `test/registry-maintenance.test.mjs`, and `test/hub-cli.test.mjs`

The backup method is `VACUUM INTO` on the running WAL-backed connection. The
result is validated as a standalone SQLite image with `integrity_check`,
`foreign_key_check`, schema validation, row counts, and a deterministic
non-secret business-state digest. Restore requires an explicit writer-quiescence
assertion, validates the standalone image, stages it, atomically publishes it,
and quarantines the replaced database and any stale `-wal`/`-shm` sidecars. A
live `.db` copy is never treated as a consistent backup.

## Latest executable evidence

The dedicated drill writes raw, sanitized evidence to:

```text
data/stage7-drill-evidence.json
```

Run it with:

```text
npm run stage7:drill
```

Latest clean-candidate drill metadata:

```text
runId: stage7-20260902020402992-14868
candidate observed: f4edab683705dd3b84147b450f43502ff6bd941d
branch: chore/v0.3-stage7-operational-hardening
cleanWorktreeBefore: true
```

The drill directly demonstrated:

- empty fresh database with the fixed v3 tables and WAL;
- explicit startup maintenance invocation and integrity validation;
- v1, v2, and v3 file-backed migration to schema 3, with state preserved and
  reopen idempotency;
- unsupported-future and corrupt database rejection without source-file
  replacement;
- a standalone `VACUUM INTO` backup while the source used WAL;
- post-backup mutation visible before restore and absent after restore;
- atomic restore after the Registry writer was closed, with WAL/SHM quarantine;
- report purge, ten-day event rollup, old raw-event purge, audit purge, and
  repeated-maintenance stability.

## Evidence status by required operational scenario

Completed by the current drill or stable committed regressions:

- schema-shape, legacy migration, future-schema, malformed-schema, and corrupt
  database fail-closed behavior;
- backup/restore integrity and sidecar handling;
- fresh install and maintenance;
- retention and rollup boundaries;
- Node CLI process restart preserving nodeId/keyId and heartbeat in
  `test/node-cli-e2e.test.mjs`;
- in-process Hub/Node restart and rotation/reenrollment response-loss
  semantics in the existing Registry/Node tests.

Not yet demonstrated at the required Stage 7 process boundary:

- a real Hub child-process stop/start against the same persistent database;
- killed-process pending rotation recovery;
- killed-process pending reenrollment recovery;
- a Stage 7-specific long-downtime (>24h) restart/recovery drill.

A duplicate Hub process test was removed because its Windows teardown left a
child process and SQLite WAL lock behind; its assertions are not counted as
Stage 7 evidence. The missing scenarios remain review blockers and are not
inferred from same-process tests.

## Verification at this checkpoint

Focused Stage 7 persistence, migration, backup, maintenance, and Hub-startup regressions:

```text
33 tests / 33 pass / 0 fail
```

Full repository suite from the clean executable candidate:

```text
284 tests / 282 pass / 2 environment skips / 0 fail
```

The two skips are environment-specific: POSIX permission bits on Windows and
GNU tar behavior for Windows paths. The raw drill evidence is intentionally
ignored by `data/`; it is a run artifact, not a release claim.

## Stop point

Stage 7 is **not complete** because the required process-boundary and
long-downtime evidence listed above is missing. Continue only within the
already authorized Stage 7 scope. Stage 8 has not started and is not
authorized by this attestation.
