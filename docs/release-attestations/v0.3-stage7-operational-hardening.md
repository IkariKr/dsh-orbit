# v0.3 Stage 7 operational hardening

Status: **complete; accepted**

This document records the Stage 7 operational-hardening candidate. It is
separate from the Stage 6 mounted Gate B attestation. Stage 8 remains at its
final-review boundary: the release candidate is awaiting final review and has
not been tagged, published, or promoted.

## Scope

The candidate covers only the SOP Stage 7 scope:

- fresh file-backed Registry installation and startup maintenance;
- real file-backed v1 → current, v2 → current, and v3 → current migration;
- SQLite-consistent backup → mutation → restore;
- fail-closed unsupported, malformed, and corrupt database handling;
- persistent-state inspection, startup integrity checks, and process-boundary recovery evidence;
- operational retention, recovery, and explicit Registry-file permission evidence recorded by the Stage 7 drill.

No wire protocol, authentication/trust model, Node identity semantics, schema
contract, routing, reverse connection, fleet, terminal, or third-party plugin
scope was added.

## Implementation

- SQLite schema validation and startup classification: `src/registry/sqlite.mjs`
- SQLite operational backup/restore: `src/registry/backup.mjs`
- Hub startup fail-closed boundary: `bin/dsh-orbit-hub.mjs`
- Isolated operational drill: `scripts/registry-stage7-drill.mjs`
- Regression coverage: `test/registry-backup.test.mjs`,
  `test/registry-stage7-contract.test.mjs`, `test/registry-sqlite.test.mjs`,
  `test/registry-maintenance.test.mjs`, and `test/hub-cli.test.mjs`

The backup method is `VACUUM INTO` on the running WAL-backed connection. The
result is validated as a standalone SQLite image with `integrity_check`,
`foreign_key_check`, schema validation, row counts, and a deterministic
non-secret business-state digest. Restore requires an explicit writer-quiescence
assertion, validates the standalone image, stages it, atomically publishes it,
and quarantines the replaced database and any stale `-wal`/`-shm` sidecars. A
live `.db` copy is never treated as a consistent backup.

## Latest executable evidence

The dedicated drill writes raw, sanitized evidence to:

```text
data/stage7-drill-evidence.json
```

Run it with:

```text
npm run stage7:drill
```

Latest clean-candidate drill metadata:

```text
runId: stage7-20260902062104795-44820
candidate observed: 0785216cc552cf88d5a7b93a72e43eb40543ad82
branch: chore/v0.3-stage7-operational-hardening
cleanWorktreeBefore: true
success: true
failedPredicates: []
```

The drill directly demonstrated:

- empty fresh database with the fixed v3 tables and WAL;
- startup `integrity_check` and `foreign_key_check` before migration/WAL mutation
  and again after migration;
- valid-header business-page corruption and existing foreign-key violations
  rejected before Hub listening, with original database bytes preserved;
- v1, v2, and v3 file-backed migration to schema 3, with state preserved and
  reopen idempotency; v3 is an explicit validated no-op reopen;
- unsupported-future and corrupt database rejection without source-file
  replacement;
- a standalone `VACUUM INTO` backup while the source used WAL; its exclusive
  temporary path is created by Orbit at POSIX `0600` before SQLite writes, and
  the pre-final-chmod regression observes the populated image already private;
- post-backup mutation visible before restore and absent after restore;
- atomic restore after the Registry writer was closed, with WAL/SHM quarantine;
- explicit POSIX `0600` Registry DB/backup/restore image permissions (with the
  documented Windows permission-bit skip);
- report purge, ten-day event rollup, old raw-event purge, audit purge, and
  repeated-maintenance stability.

## Evidence status by required operational scenario

Completed by the gated drill and stable committed regressions:

- schema-shape, legacy migration, future-schema, malformed-schema, plain corrupt,
  valid-header business-page-corrupt, and existing-FK-violation fail-closed
  behavior;
- startup integrity/FK checks before migration/WAL mutation and after migration;
- source-byte preservation and no rebuild/overwrite on all required failure modes;
- backup/restore integrity, standalone `VACUUM INTO`, atomic publication, and
  sidecar quarantine;
- fresh install, startup maintenance, retention, rollup boundaries, and repeated
  maintenance stability;
- real Hub A → Hub B child-process restart against the same persistent database,
  preserving node identity, key identity, report/health state, audit evidence,
  and browser session visibility;
- killed-process rotation response-loss recovery, exact pending-key promotion,
  no third key, and no orphan node;
- killed-process reenrollment response-loss recovery, exact replay with the
  same token and nodeId, pending-intent clearing, and no orphan node;
- Stage 7-specific long-downtime process recovery: lost/contact-lost survives
  Hub restart, report upload does not heal contact, and authenticated heartbeat
  restores fresh and clears the alert;
- Node CLI process restart preserving nodeId/keyId and heartbeat in
  `test/node-cli-e2e.test.mjs`;
- in-process response-loss and lifecycle regressions retained as complementary
  coverage, not substituted for the process-boundary evidence above.

The process harness uses direct child handles, complete `close` waits, a stable
loopback proxy URL, and Windows `taskkill.exe /PID /T /F` fallback only for the
known child PID. No pattern-based process termination is used.

## Verification at this checkpoint

Focused Stage 7 persistence, migration, backup, maintenance, Hub-startup,
process-boundary, and drill-contract regressions:

```text
24 tests / 23 pass / 1 Windows permission skip / 0 fail
```

Full repository suite from the clean executable candidate:

```text
289 tests / 286 pass / 3 environment skips / 0 fail
```

The three skips are environment-specific: two POSIX permission-bit checks on
Windows and GNU tar behavior for Windows paths. The gated drill itself reports:

```text
requiredPredicates: all true
failedPredicates: []
success: true
```

The raw drill evidence is intentionally ignored by `data/`; it is a run
artifact, while this attestation is the durable sanitized release claim.

## Stop point

Stage 7 is **complete; accepted**. The gated drill passed with all required
predicates true, the final worktree was clean, and the Stage 7 evidence is
accepted. Stage 8 remains at its final-review boundary: the release candidate
is awaiting final review and has not been tagged, published, or promoted.
