# v0.4 Stage 8 Independent Final Review — Closure Record

Status: **FINAL REVIEW PASS — v0.4 STAGE 8 CLOSED; v0.4 ENGINEERING ACCEPTANCE = PASS**

Date: 2026-09-19

## Review input

- Accepted construction candidate:
  `cf38544e6a25cbfc2504d3fb833d09557e9f9ae2`
  ("fix(stage8): make browser cleanup nonblocking"), branch
  `chore/v0.4-stage8-final-review-remediation-r1`.
- Accepted evidence-only closure (direct child of the candidate):
  `9891ab858a9c953a211978580910efcc2158bcd7`
  ("docs(release): record E8.7 mounted final evidence for v0.4.0-rc.2").
  The closure commit is pushed; local and remote SHA are an exact match with
  0/0 divergence on `origin/chore/v0.4-stage8-final-review-remediation-r1`.
- Attestation: `docs/release-attestations/v0.4.0-rc.2.md` (evidence phase
  E8.7; mounted runId
  `525260cd-0085-4565-af82-dfc63eb0f142`; 25/25 mounted matrix PASS).
- Reviewer: independent reviewer process, read-only, re-executed every
  verification mechanically (no reliance on prior recorded results).

## Scope of the independent review (all mechanically re-verified)

1. Authorization lineage — `S8-CONSTRUCTION-20260918-A1`; ancestors
   `6f766acf` (authorization record), `de8eb467` (accepted runtime base),
   `2a1f41a7` (governance predecessor) verified against the candidate.
2. Candidate scope — full `6f766acf..cf38544e` diff inside the candidate
   allowlist; no `src/**`, `bin/**`, `ui/**`, or forbidden files.
3. Candidate tests — Stage 8 contracts re-run in a clean detached worktree
   at the candidate: 14/14 pass, 0 fail.
4. 25-field mounted matrix — raw, smoke, and attestation all exactly 25
   fields, all PASS.
5. Manifest hash/bytes — all six artifacts re-hashed (LF bytes) and
   re-counted; every SHA-256 and byte count matches `manifest.json`; smoke
   `rawEvidenceSha256`/`rawEvidenceBytes` match the raw record;
   no closure self-reference.
6. runId / node IDs — `525260cd-0085-4565-af82-dfc63eb0f142`,
   `node_5fb58161ac97b584ef86ee8e8f00b5e0`,
   `node_a956ebdea5a4d97b01b1fdd3ba000743` consistent across raw, smoke,
   manifest, attestation; quarantined historical lineage identifiers absent
   from all closure files.
7. DSH identity — `0.1.1-rc.2` /
   `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e` /
   `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`
   consistent everywhere.
8. Browser challenge — `cb481fad3d99b2c0844bf6c9e72649ec478e1c71d25e793a3fef95bf41dc39c6`
   mechanically derived by the runner (`sha256(BROWSER_CHALLENGE)`) and
   consistent in all bindings.
9. Browser producer — `runner-owned-firefox-selenium` everywhere.
10. TLS CA/leaf fingerprints —
    CA `B0:1D:FA:2C:6D:66:2D:D5:EE:03:AE:EA:AB:58:4A:F6:0D:97:1E:8F:DB:96:2D:52:16:78:33:C7:5D:C6:B4:D7`,
    leaf `ED:E8:A2:D8:A6:20:D3:D2:75:61:B5:09:50:2C:7B:D1:0E:6D:67:6B:4B:4B:1F:F7:3F:DB:50:C8:45:DB:EA:21`,
    consistent in raw, smoke, manifest, attestation.
11. Gateway binding — `https://127.0.0.1:8543` everywhere.
12. NSS trust mode — `firefox-profile-nss`; bridge verified to use NSS
    `certutil` only (`-N --empty-password`, `-A -t C,,`, `-L` verify) and to
    never touch Windows certificate stores.
13. Closure direct-child — `9891ab8^ == cf38544e`, single parent.
14. Closure path purity — diff is exactly the attestation plus
    `test/evidence/stage8/**` (8 files); `git diff --check` clean; worktree
    fully clean including untracked files.
15. Closure full tests — focused Stage 8 contracts 14/14; `npm run check`
    392 tests / 387 pass / 0 fail / 5 skip (exit 0); public-tree validation
    passed; the closure-aware gate produced no "candidate paths outside
    allowlist" false failure.
16. Residue hygiene — `data/**`/`secrets/**` ignored residue 0;
    `dsh-orbit-firefox-*` temporary profiles 0; `Cert:\CurrentUser\Root`
    and `Cert:\LocalMachine\Root` drill CA count 0; drill Docker containers
    0; runner/orphan browser processes 0 (two `geckodriver` orphans from
    earlier pre-candidate attempts were found during closure hygiene and
    terminated before this review; re-check = 0); untracked files 0.
17. Attestation discipline — no closure SHA self-reference; dispositions
    record Final Review pending at closure time, tag/release and production
    promotion not authorized and not performed.

First review round rejected an earlier closure attempt
(`41101e852a636863001a26ad13d7fd118097bf04`, since force-replaced) because
its attestation carried quarantined historical lineage identifiers; the
corrected closure `9891ab8` removed them and re-passed the full battery.
That rejected attempt is quarantined historical construction and is not a
permitted base for any future work.

## Formal dispositions

- **v0.4 Stage 8 = CLOSED**
- **v0.4 engineering acceptance = PASS**
- Construction candidate: **PASS** (`cf38544e6a25cbfc2504d3fb833d09557e9f9ae2`)
- Mounted final evidence: **PASS** (E8.7, 25/25, runId `525260cd-…`)
- Independent Stage 8 Final Review: **PASS** (P0 = 0, P1 = 0, P2 = 0)

These dispositions separate engineering acceptance from the following,
each of which remains **independently authorized and not yet authorized**:

- Tag / release publication — separate authorization; **not authorized,
  not performed**.
- Production promotion — separate authorization; **not authorized, not
  performed**; the documented promotion/rollback plan
  (`docs/sop/v0.4-production-promotion-rollback-plan.md`) remains
  plan-only, and its staging rollback rehearsal belongs to that separately
  authorized phase.
- DNS cutover — separate authorization; **not authorized, not performed**.

v0.5 construction is **not** gated on production promotion or DNS cutover.

## v0.5 baseline designation

The **only** lawful starting point for v0.5 work is the accepted v0.4
closure:

```text
v0.5 baseline (accepted v0.4 closure):
9891ab858a9c953a211978580910efcc2158bcd7
```

v0.5 work must not fork from:

- any historical Stage 8 branch or worktree (including
  `chore/v0.4-stage8-construction-s8-construction-20260918-a1`,
  `chore/v0.4-stage8-e8.4-mounted-pass`,
  `chore/v0.4-stage8-authority-boundary-c8.4`,
  `chore/v0.4-stage8-c8.2.4`, the `dsh-orbit-stage8-*` directories, and the
  `.devspace` detached worktrees);
- the failed pre-remediation closure attempt `41101e8…` or the superseded
  E8.6 lineage (`4c6b033…` / `a593d46b…` / `f8d07c7`);
- any pre-final-review candidate;
- any experimental worktree.

Everything else in the repository history is either accepted ancestry of
this baseline or quarantined historical construction.
