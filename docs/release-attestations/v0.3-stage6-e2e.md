# Stage 6 mounted-deployment evidence — Review Gate B HOLD

Review status: **Stage 6 live evidence complete after remediation rerun; Review Gate B HOLD**
SOP: `docs/sop/v0.3-registry-mvp-multistage-sop.md`, Stage 6
Stage 7: **NOT STARTED**

This is an evidence-only record for the completed isolated mounted drill. It is
not Gate B approval and does not authorize Stage 7.

## Exact candidate and run binding

- Remediation commit: `2f713e8bbd023ac49080bd2da44023c4a31323db`
- Candidate worktree at drill start: clean
- Captured status: `git status --porcelain` was empty
- Run ID: `0ab127dc-1731-4791-a842-7262475f703c`
- Run window: `2026-09-01T11:56:02.221Z` to `2026-09-01T12:03:17.778Z`
- Persistent SQLite mount: host `data/orbit-drill` → Hub `/data/orbit`
- Database path: `/data/orbit/registry.db`

The compose build ran from the exact clean remediation commit. The driver
captured image identities from the running containers after compose build/start,
not from mutable tags before startup. The complete raw result is retained in
the ignored local artifact `data/drill-evidence.json` until the next Review.

## Image and runtime identities

- Hub image `dsh-orbit-registry:drill`
  - running-container image ID: `sha256:5ebc31766056c1e7e7c8c663bd294da9b3119a7549e340bbd92da3690e0460f4`
  - repository digest: unavailable in local store
  - created: `2026-09-01T11:56:04.427001937Z`
- Hub base image: `node:22.14.0-bookworm-slim`
  - pinned digest: `sha256:1c18d9ab3af4585870b92e4dbc5cac5a0dc77dd13df1a5905cea89fc720eb05b`
- DSH A image `dsh-orbit:dsh-drill-a`
  - running-container image ID: `sha256:2603b543111cfd531e399e7150f7fdbb2efb9df03805352e369045858a6f9069`
  - repository digest: unavailable in local store
  - created: `2026-09-01T11:56:08.097744189Z`
- DSH B image `dsh-orbit:dsh-drill-b`
  - running-container image ID: `sha256:39541bc8137ab3b911ecbf705b398198b80275c15848901ff7f93f815aa3a078`
  - repository digest: unavailable in local store
  - created: `2026-09-01T11:56:08.095862336Z`
- Caddy image: `caddy:2-alpine`
  - running-container image ID: `sha256:af555904a0961945f16bb323a501457b13a4f7e9bde969b145b97da80b38ecbe`
  - repository digest: `caddy@sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648`
  - created: `2026-06-22T20:09:05.216443494Z`
  - runtime version: `v2.11.4`
- DSH version: `0.1.1-rc.2`
- Hub listener policy: `127.0.0.1:5445` (loopback-only)
- DSH A/B long-lived runtime UID: `10001:10001`
- Root use: limited to the separate one-shot `dsh-a-init` and `dsh-b-init` services

Container IDs captured by the driver:

- Hub: `30917490b29f8ab828a3fc49b6e3667f2f57a517dd5dcf65e4cee42f8d9814f5`
- Caddy: `1995805983e4413979b50b1efe7c080bd21f9fe70985b2f72de99d7e46563ee7`
- DSH A: `727db22b5ec44a49f488792788dc0fc7efd5bec91ee3de4f8b3446df8060b9c5`
- DSH B: `b91a48e4e166a27d383421dd864f499d4868b1df7c99d7ff737ed1c703b82dd3`

## Mounted topology and private machine boundary

```text
Browser --trusted HTTPS :8443--> Caddy (shared Hub network namespace)
                                  ├── browser paths -> Hub 127.0.0.1:5445
                                  └── /api/v1/* -> 403, never proxied
DSH A/B (independent bridge namespaces)
    └── registry-hub:5446 -> private machine-ingress sidecar
                              └── Hub 127.0.0.1:5445
```

The machine-ingress port was not published to the host. Its exact raw
request-target allowlist is:

- `/api/v1/enroll`
- `/api/v1/heartbeat`
- `/api/v1/report-upload`
- `/api/v1/credential-rotate`
- `/api/v1/reenroll`

The remediation tests prove that the ingress rejects query strings, encoded
paths, dot-segment targets, browser paths, and unknown routes before any
upstream request. The live gateway check also proved that browser `/api/v1/*`
requests receive 403 and are never proxied. The private machine boundary
therefore remains distinct from the browser gateway surface.

## TLS and trusted browser evidence

- TLS validation: **enabled** for Node clients and browser walkthrough
- Drill CA subject: `CN=dsh-orbit-drill-ca`
- CA SHA-256 fingerprint: `18:5E:D0:23:47:E1:B4:CB:90:80:57:1F:70:52:35:76:87:EF:EF:3A:2B:6B:29:D0:69:F9:E4:20:61:FC:4B:D3`
- Leaf SHA-256 fingerprint: `75:9B:74:E9:C6:17:A5:79:91:67:F6:B6:53:9D:D5:D6:DD:E1:09:C1:E9:8E:C5:84:E3:C0:96:DE:3E:D9:27:2D`
- Leaf SANs: `127.0.0.1`, `dsh-a.test`, `dsh-b.test`
- Caddy validation: `Valid configuration`
- CurrentUser Root trust: the CA was explicitly installed and verified by
  thumbprint before the browser walkthrough; the leaf was not installed as a
  trust root
- Browser bootstrap checkpoint: passed
  - trusted HTTPS
  - Basic Auth
  - operator session bootstrap
  - enrollment-token mint through the UI
  - plaintext-once element observed without recording its value
  - plaintext-once element remained after a Nodes/Tokens refresh
- Browser live checkpoint: passed
  - live A/B Nodes list observed
  - independent health dimensions observed
  - `lastHeartbeat` observed
  - A node detail, latest report, capability evidence, and event history observed

The checkpoint files and this document contain only run bindings, statuses,
fingerprints, and Node IDs. They contain no token, private key, or plaintext
secret.

## Real compatibility evidence

Both reports were generated by the real DSH/Orbit verification pipeline,
converted through `createCompatibilityReport()`, copied into the mounted DSH
volume, and uploaded through the real `upload-report` CLI. No hand-built
all-pass report was used.

- A report outcome: `pass`
- B report outcome: `pass`
- Required checks for each node: `runtimeReadiness`, `globalPatch`,
  `profilePatch`, `settingsRead`, `settingsNoopWrite`, `authorizationSmoke`,
  `sessionResume`, and `webPluginRoutes` — all `pass`
- v0.3-deferred checks: `longLivedTransport`, `terminalFence`, and
  `terminalPtty` — `not_run` by design
- A derived capabilities: `sessions.resume`, `settings.remote`, `web.routes`
- B derived capabilities: `sessions.resume`, `settings.remote`, `web.routes`

Real DSH session persistence was verified through `session.create` followed by
`session.list` for both nodes before the compatibility sequence.

## Node identities and lifecycle result

- A Node ID: `node_0b832a5c638cc31823da25619e8d32e4`
- B Node ID: `node_28850d17335748c89580efdd4a4aaa1f`

| Scenario step | Result |
| --- | --- |
| Persistent Hub + SQLite startup | **PASS** |
| Real Caddy startup and validation | **PASS** |
| Unauthenticated browser request | **PASS** — 401 before Hub |
| Browser `/api/v1/*` machine surface | **PASS** — 403, not proxied |
| Exact private machine-ingress boundary | **PASS** — fixed raw route allowlist; query, encoded, dot-segment, and unknown variants denied before upstream |
| A/B production-default DSH entrypoints | **PASS** — healthy, non-root |
| A/B private enrollment | **PASS** |
| A/B real report upload and capability derivation | **PASS** — three capabilities each |
| A/B `registryContact=fresh` | **PASS** |
| A/B `lastHeartbeatAt` surfaced | **PASS** |
| Gateway restart | **PASS** — new session and live post-restart state matched pre-restart state |
| Stop only A with owned PID sidecar | **PASS** |
| A accelerated transition to `stale` | **PASS** — controlled clock represented three missed 60-second beats |
| A accelerated transition to `lost` + `contact-lost` | **PASS** |
| B during A outage | **PASS** — remained `fresh`, three capabilities, no alerts |
| Reset aging override before reconnect | **PASS** |
| Real maintenance tick after reset | **PASS** — A remained `lost` + `contact-lost` until authenticated heartbeat |
| A authenticated reconnect heartbeat | **PASS** — fresh again and alert flags cleared |
| Delete A through browser management surface | **PASS** — tombstoned with explicit requestId |
| A machine denial and local state | **PASS** — local state became `revoked` |
| Tombstone-bound A reenrollment | **PASS** — original Node ID restored |
| A after reenrollment | **PASS** — active and fresh |
| Final B isolation | **PASS** — B remained fresh |
| Cleanup | **PASS** — owned Node daemons stopped and compose resources removed |

The live driver result was `success: true`. Raw evidence remains at
`data/drill-evidence.json` for the next Review; it has not been deleted or
rewritten. This document omits all credentials and plaintext token values.

## Controlled aging record

Production RFC-0009 thresholds were unchanged:

- default heartbeat cadence: 60 seconds
- three missed beats: `stale`
- 24 hours without contact: `lost` plus `contact-lost`

The isolated drill used only the private node-scoped Hub contact-aging clock:

- mode: `controlled-accelerated-contact-aging`
- A disconnect wall clock: `2026-09-01T12:01:00.605Z`
- A stale accelerated clock: `2026-09-01T12:03:23.487Z`
- A lost accelerated clock: `2026-09-02T12:00:23.487Z`
- Aging override reset before reconnect: **yes**
- Reset wall clock: `2026-09-01T12:01:39.761Z`
- A remained `lost` + `contact-lost` across a real maintenance tick after reset: **yes**
- A returned to `fresh` only after authenticated heartbeat: **yes**

Authentication timestamps, signatures, real Node heartbeat timers, report
timestamps, retention, audit timestamps, and B's heartbeat path remained on
wall-clock semantics. No SQLite timestamp was edited directly, and no TLS
validation bypass was used.

## Gate B disposition

The remediation rerun from `2f713e8` completed the previously identified
maintenance aging, private-ingress request-target, and image-provenance fixes.
The live evidence is complete for this candidate, but the process remains at
**Review Gate B HOLD** pending operator review of the complete evidence package:

- exact raw private machine-ingress boundary and browser separation;
- browser trust and one-time plaintext handling;
- production-default non-root deployment and root-only initialization;
- real compatibility evidence and capability authority;
- restart, accelerated aging, reset-tick persistence, reconnect, deletion,
  revocation, and tombstone-bound reenrollment;
- A/B isolation and cleanup;
- running-container image provenance.

Stage 7, backup/restore work, v0.4/v0.5 routing, reverse connections,
multi-node sessions, fleet execution, and third-party plugin expansion were not
started.
